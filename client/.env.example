# Leave empty locally to use /api through the Vite development proxy.
VITE_API_URL=""

# Set this BEFORE building the frontend on your hosting service:
# VITE_API_URL="https://your-backend.example.com/api"
# This URL is public. Never put DATABASE_URL or JWT_SECRET in the frontend.
